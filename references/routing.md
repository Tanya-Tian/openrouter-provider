# Routing Notes

This reference file is intentionally short in the first draft.

Use it later to document:

- direct OpenRouter vs Cloudflare AI Gateway base URL examples
- image route fallback rules
- capability checks by modality
- common provider error categories
